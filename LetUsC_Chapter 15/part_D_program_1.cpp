#include<stdio.h>
#include<stdlib.h>
#include<string.h>
#include"../my_functions.h"

int main()
{
	char str[30];
	int a, b;
	printf("Enter any string :");
	gets(str);
	printf("enter starting character and number of characters to be copied :");
	scanf("%d %d",&a,&b);
	char copied[]
}

